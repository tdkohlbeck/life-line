import {
  selectTestContainer,
} from '../selectors';
import { fromJS } from 'immutable';
import expect from 'expect';

const selector = selectTestContainer();

describe('selectTestContainer', () => {

});
